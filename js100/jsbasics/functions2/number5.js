function catAge(num) {
	let age;
  if (num === 0) {
  	 age = num; 
  } else if (num === 1) {
  	 age = 15;
  } else if (num === 2) {
  	age = 24;
  } else {
  return	age = (24 + ((num - 2) * 4));
  }
  return age;
}
