/*
It is true never runs because for (let prop in obj) going thur that key names in the object. All key names are strings.
The if statement is comparing the key name 'true' with the boolean true. Since 'true' !== true  the if statement does
not execute;
*/
