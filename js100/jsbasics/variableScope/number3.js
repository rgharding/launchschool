if (true) {
  let myValue = 20;
}

console.log(myValue);

// Reference error. myValue is not defined;  I thought it would say not declared. Either way it is scoped in the if block so the console cannot access it.
