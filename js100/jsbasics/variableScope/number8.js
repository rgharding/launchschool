/*
This will log 2. The value of variable b is passed as argument to the fucuntion. The paramenter of that function shares a letter with in outer scope variable, 
but the variable a (the parameter) shadows the global variable a, so that our function only uses that a parameter and consoles the aurgument that 
has been passed to it. 
*/
